import { describe, expect, it } from "vitest";

// Placeholder smoke test to confirm the Vitest runner is wired up correctly.
// Later phases will replace/extend this with real component and content tests.
describe("test runner", () => {
  it("runs", () => {
    expect(true).toBe(true);
  });
});
